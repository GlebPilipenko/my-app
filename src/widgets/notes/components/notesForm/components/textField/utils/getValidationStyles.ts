export const q = () => {};

// import cn from 'classnames';
// import style from '../TextField.module.css';
//
// export const getValidationStyles = (
//   propsCount: number, maxLength: number, minLength: number
// ) => {
//   const error = (
//     (propsCount !== 0) &&
//     ((propsCount > maxLength) || (propsCount < minLength))
//   );
//
//   const countStyle = cn(style.error__count, {
//     [`${style.no_error__count}`]: !error,
//   });
//   const messageStyle = cn(style.error__message, {
//     [`${style.no_error__message}`]: !error,
//   });
//
//   return {error, countStyle, messageStyle};
// };
