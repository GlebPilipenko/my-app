import {WidgetTitles} from 'src/enums';

export type PropsType = {
  city?: string;
  coords?: string;
  styles?: string;
  masonry?: string;
  carousel?: string;
  country?: string;
  app?: WidgetTitles;
};
